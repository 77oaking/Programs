#include <stdio.h>
#include <stdlib.h>

int main()
{

    /// 1 dimentional array
    /**< int nums[how many position do you want]={each position is divided by a comma} if left {} that means each position is held 0*/

    int nums[]={};
    system("pause");
    return 0;
}
